Wallet {{ report.wallet }} on {{ report.asset_pair }} received a risk score of {{ report.risk_score }} ({{ report.verdict }}), but no single detector signal was strong enough to support a specific narrative.{% if top_features %} The highest-contributing factors were {% for f in top_features %}{{ f.display }}{% if not loop.last %}, {% endif %}{% endfor %}, though none individually crossed a high-confidence threshold.{% else %} Insufficient feature attribution is available to characterize the flag further.{% endif %} Manual review is recommended before any action is taken.
